<a id="plugins.aea-ci-helpers.aea_ci_helpers.generate_pkg_list"></a>

# plugins.aea-ci-helpers.aea`_`ci`_`helpers.generate`_`pkg`_`list

Script to generate a markdown package table.

<a id="plugins.aea-ci-helpers.aea_ci_helpers.generate_pkg_list.get_packages"></a>

#### get`_`packages

```python
def get_packages() -> Dict[str, str]
```

Get packages.

<a id="plugins.aea-ci-helpers.aea_ci_helpers.generate_pkg_list.generate_table"></a>

#### generate`_`table

```python
def generate_table() -> None
```

Generates a markdown table containing a package list

